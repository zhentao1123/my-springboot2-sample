package ${basePackage}.${repositoryPackage};

import ${entityPkClass};
import org.springframework.data.jpa.repository.JpaRepository;
import ${basePackage}.${entityPackage}.${entityClassName};

public interface ${entityClassName}Repository extends JpaRepository<${entityClassName}, ${entityPkClassName}>{
	
}